import  logging
logging.basicConfig(filename ="test.log",level=logging.INFO)

logging.info("this is the trial version")
logging .warning("all warning")
l =[1,2,3,4,45]
for i in l:
    logging.info(i)

